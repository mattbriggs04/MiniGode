[
  {
    "id": "trapping-rain-water",
    "slug": "trapping-rain-water",
    "title": "Trapping Rain Water",
    "difficulty": "hard",
    "tags": ["array", "two-pointers", "dynamic-programming"],
    "statement": [
      "You are given `height`, where each value represents the height of a vertical bar with width `1`.",
      "After rain falls, water can be trapped between the bars. Return the total amount of trapped water."
    ],
    "constraints": [
      "1 <= len(height) <= 20000",
      "0 <= height[i] <= 100000"
    ],
    "examples": [
      {
        "input": "height = [0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1]",
        "output": "6"
      },
      {
        "input": "height = [4, 2, 0, 3, 2, 5]",
        "output": "9"
      }
    ],
    "signature": {
      "functionName": "trap_rain_water",
      "parameters": [
        { "name": "height", "type": "list[int]" }
      ],
      "returnType": "int"
    },
    "starterCode": "class Solution:\n    def trap_rain_water(self, height: list[int]) -> int:\n        pass\n",
    "publicTests": [
      {
        "args": [[0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1]],
        "expected": 6,
        "description": "classic basin layout"
      },
      { "args": [[4, 2, 0, 3, 2, 5]], "expected": 9, "description": "multiple valleys" }
    ],
    "hiddenTests": [
      { "args": [[1, 0, 2]], "expected": 1, "description": "single basin" },
      { "args": [[3, 0, 1, 3, 0, 5]], "expected": 8, "description": "water across several pits" },
      { "args": [[5, 4, 1, 2]], "expected": 1, "description": "descending wall with a tail rise" }
    ]
  },
  {
    "id": "minimum-window-substring",
    "slug": "minimum-window-substring",
    "title": "Minimum Window Substring",
    "difficulty": "hard",
    "tags": ["string", "hash-table", "sliding-window"],
    "statement": [
      "Given strings `s` and `t`, return the smallest substring of `s` that contains every character in `t`, including duplicates.",
      "If no such substring exists, return an empty string."
    ],
    "constraints": [
      "1 <= len(s), len(t) <= 100000",
      "`s` and `t` consist of English letters."
    ],
    "examples": [
      {
        "input": "s = \"ADOBECODEBANC\", t = \"ABC\"",
        "output": "\"BANC\""
      },
      {
        "input": "s = \"a\", t = \"aa\"",
        "output": "\"\"",
        "explanation": "The source string does not contain enough copies of `a`."
      }
    ],
    "signature": {
      "functionName": "min_window",
      "parameters": [
        { "name": "s", "type": "str" },
        { "name": "t", "type": "str" }
      ],
      "returnType": "str"
    },
    "starterCode": "class Solution:\n    def min_window(self, s: str, t: str) -> str:\n        pass\n",
    "publicTests": [
      { "args": ["ADOBECODEBANC", "ABC"], "expected": "BANC", "description": "classic sample" },
      { "args": ["a", "a"], "expected": "a", "description": "single-character match" }
    ],
    "hiddenTests": [
      { "args": ["a", "aa"], "expected": "", "description": "not enough copies of a required character" },
      { "args": ["ab", "b"], "expected": "b", "description": "window can end at the last character" },
      { "args": ["bba", "ab"], "expected": "ba", "description": "minimum window may overlap repeated prefixes" }
    ]
  }
]
